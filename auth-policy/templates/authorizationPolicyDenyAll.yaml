{{- range $key, $spec := .Values.namespacePolicy }}
{{- if $spec.defaultDeny }}
apiVersion: "security.istio.io/v1beta1"
kind: "AuthorizationPolicy"
metadata:
  name: "deny-all"
  namespace: {{ $key | quote }}
spec:
  {}
---
{{- end }}
{{- end }}